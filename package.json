{
  "name": "ld48",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "tailwind": "npx postcss src/styles/tailwind.css -o public/styles/style.css",
    "dev": "npx pug-cli src/pages -o public -w -b src",
    "build": "NODE_ENV=production npx pug-cli src/pages -o public -b src && NODE_ENV=production npx postcss src/styles/tailwind.css -o public/styles/style.css"
  },
  "author": "Astavie",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.12",
    "postcss-cli": "^8.3.1",
    "pug": "^3.0.2",
    "pug-cli": "^1.0.0-alpha6",
    "tailwindcss": "^2.1.2"
  }
}